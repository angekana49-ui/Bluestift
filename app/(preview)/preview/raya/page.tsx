"use client";

import { RayaApp } from "@/components/raya/raya-app";

/** Design-shell preview: the full Raya student app (chat/rooms/tools/kernel/settings). */
export default function RayaPreview() {
  return <RayaApp />;
}
