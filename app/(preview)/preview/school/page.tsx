"use client";

import { SchoolsApp } from "@/components/school/schools-app";

/** Design-shell preview: the full Schools admin/teacher app (Admin & Prof roles). */
export default function SchoolPreview() {
  return <SchoolsApp />;
}
