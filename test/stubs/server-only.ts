// Test stub for the `server-only` package, which throws when imported outside a
// React Server Component bundle. In unit tests we import server modules directly,
// so this no-op replacement lets them load. Aliased in vitest.config.ts.
export {};
